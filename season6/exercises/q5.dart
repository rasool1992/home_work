// Question 5:
// Create a program with a boolean isMember = false and an integer points = 120. If isMember is true and points >= 100, print 'Eligible for reward'. Otherwise, print 'Not eligible'.
// ignore_for_file: dead_code

void main() {
  bool isMember = false;
  int points = 120;
  if (isMember && (points >= 100)) {
    print('Eligible for reward');
  } else {
    print('Not eligible');
  }
}
